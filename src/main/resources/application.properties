# DataSource configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/emp-db
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Gopi@2003

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# MySQL Console configuration (optional)
# Remove or comment out the following two lines if you don't want to enable the MySQL console
# spring.datasource.hikari.console.auto-commit=true
# spring.datasource.hikari.console.sql-statement-timeout=1000

# Server port
server.port=8082
